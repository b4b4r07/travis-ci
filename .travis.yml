language: c
os:
    - linux
    - osx

env:
  global:
    - GOPATH=~
    - DOTPATH=~/.dotfiles

sudo: required

before_install:
    - sudo apt-get install shellcheck

before_script:
    - curl -L dot.b4b4r07.com | bash
    - cd $(dotpath)
    - make init

script:
    - make --silent test
